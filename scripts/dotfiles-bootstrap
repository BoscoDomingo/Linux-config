#!/usr/bin/env bash
# Run Home Manager bootstrap from any directory.
# On PATH via ~/.local/bin/scripts after Home Manager activation.
#
#   dotfiles-bootstrap
#   HOST=ubuntu dotfiles-bootstrap
set -euo pipefail

REPO="${DOTFILES_REPO:-$HOME/dotfiles}"
exec bash "$REPO/nix/bootstrap.sh" "$@"
